const theme=document.createElement('link');theme.rel='stylesheet';theme.href='mkc-inspired.css';document.head.append(theme);const menu=document.querySelector('.menu');const nav=document.querySelector('.header nav');if(menu)menu.addEventListener('click',()=>{const open=nav.classList.toggle('show');menu.querySelector('span').textContent=open?'−':'+';if(open)nav.style.cssText='display:flex;position:absolute;top:68px;left:0;right:0;z-index:10;flex-direction:column;align-items:stretch;gap:0;padding:12px 6vw 18px;background:#fff';else nav.removeAttribute('style')});document.querySelectorAll('[data-project-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-project-filter]').forEach(b=>b.classList.remove('active'));button.classList.add('active');document.querySelectorAll('.project-item').forEach(card=>card.hidden=button.dataset.projectFilter!=='all'&&card.dataset.status!==button.dataset.projectFilter)}));
